# AI or not AI — 设计文档

> **这是一份活文档。** 任何设计层面的改动都要同时更新这里和 `CHANGELOG.md`。
> 设计意图与代码不一致时，以本文档的"为什么"为准，并修代码。

CRIG Ontdekt（UGent 癌症研究所公众科普活动）的网页科普游戏：左右两张图，选出哪张是 AI 生成的。

---

## 1. 为什么做这个

活动现在用一份 PowerPoint 做同样的互动（`20260917_CRIG_Ontdekt_AIorNot.pptx`）。拆包核对后的结论是：**那份 deck 里没有游戏，只有图。**

| 事实 | 证据 |
|---|---|
| 整份 deck 只有 **6 个英文词**（"AI or not images" + 日期） | `docProps/app.xml` 报 `Words=6` |
| 7 组图对（slides 2–8）+ 5 张未配对 AI 图（slides 10–14） | 19 个 `ppt/media/*` |
| **7 组全部是 PNG(AI) 在左、JPEG(真) 在右** | 按 `<a:off x>` 坐标排序；rel 顺序不等于视觉位置 |
| AI 图约 1526×1024，真图约 3713×2475 | 格式与分辨率双重泄露 |
| slide 2 两侧宽高比差 14% | 3543/2362 = 1.5000 vs 1437/1095 = 1.3123 |
| 仅 `image1.jpg` 带 EXIF（Canon EOS 5DS R / Photoshop CS6 / 2017-01-12） | 其余 6 张真图 EXIF 为 0 条 |
| `image1.jpg` 有 GPS IFD，但**只有 `GPSVersionID`，无经纬度** | 是元数据泄露，不是位置泄露 |
| 答案键**不在文件里** | 只能从格式/位置反推 |

**没有规则、没有计分、没有难度分级、没有讲解页、没有备注**——现有玩法全靠主持人现场口头讲。

**并且这份 deck 目前可以被一条规则通关：永远选左边。** 如果已经用它讲过，观众学到的是位置规律，不是识图能力。

所以要做的不是"把 PPT 搬到网页"，而是：**把这套靠口头讲的互动，做成一个自带规则、难度和讲解、能独立运行的展项。**

### 目标

- 当天在展台**离线**跑（触摸、全屏、自动重置、排队换人快）
- 之后挂在公网让人自己玩（手机/桌面）
- **同一份代码**支撑这两种用法
- 全年龄：儿童不读字也能玩，成人能得到真正的知识
- 每题揭晓后都教一条**可迁移**的识别线索

---

## 2. 硬约束

### 2.1 技术约束（由 `file://` 倒推，不是偏好）

kiosk 必须能**从磁盘双击 `index.html` 打开**。`file://` 下被 CORS 拦掉的东西一律不能用：

| 不能用 | 原因 | 替代 |
|---|---|---|
| `<script type="module">` | opaque origin 被拦 | 挂 `window` 的经典 script（`defer` 保序） |
| `fetch()` 本地 JSON | 同上 | 题库是赋值全局变量的 `.js` 文件 |
| `@font-face` | 字体跨源加载被拦 | 系统字体栈 `system-ui, "Segoe UI", Roboto, sans-serif` |
| 直接读写 `localStorage` | 可能抛异常，且所有 file-origin 页面共享 | 经 `util.storage` 包装，失败退回内存 |

推论：**零依赖、零构建、无框架、无打包。**

### 2.2 `@font-face` 不可用对视觉的连带影响

「扁平化小清新」不能靠自定义字体实现。清新感只能来自：**字重**（Segoe UI Light）、**字距**、**行高**、**留白**、**圆角**、**低饱和填充**。

同时——扁平化 + 马卡龙色的主要风险是**对比度**：**浅色只能做填充，文字必须用深墨色**，否则 AA 不过。`tokens.css` 里颜色令牌分 `*-fill`（可浅）与 `*-ink`（必须深）两组，就是这个原因。

### 2.3 内容约束

- 题材：**生物医学为主 + 少量通用题材调剂**
- 真图来源：**研究者自己的照片**（显微镜图 / 实验照片）
- 一局题量：`config.js` 可调，kiosk 与 web 分开
- 展台节奏：**作答限时 20 秒**，教学面板不限时

---

## 3. 三个关键判断

### 3.1 图片统一重编码不是美化，是修 bug

现素材有三条**独立**的作弊路径：**格式**（真=JPEG / AI=PNG）、**分辨率**（3713 vs 1526）、**元数据**（相机 EXIF vs 无）。必须全部堵死：统一 WebP + 固定长边 + **固定宽高比** + 剥 EXIF。

固定宽高比是因为 slide 2 那 14% 的差异会让两张卡在页面上大小不同——**尺寸差本身就是提示**。

这恰好就是用户要的难度轴③（抹平线索）。

### 3.2 生物医学题材有一个反直觉的陷阱

**外行看真实显微镜图，本来就分不出真假。** 如果 AI 图做得太像，题目会退化成抛硬币——**玩家答错但学不到东西，比答对更糟。**

所以生物医学题的「破绽」必须是**外行可见**的那种：

| ✅ 好破绽（判据是逻辑） | ❌ 坏破绽（判据是专业知识） |
|---|---|
| 细胞结构不可能（有丝分裂画成对称装饰图案） | "这个染色方案不是 HE 染色" |
| 染色/荧光分布不合物理（信号没有对应细胞器） | "这个蛋白定位不准确" |
| 几何自相矛盾（比例尺与细胞尺寸冲突） | "这个放大倍数下不该有这个结构" |
| 计数不对（染色体数目、细胞核数目） | "细胞形态不像这个细胞系" |

**这条要进题库编写规范**，否则收上来的题会有一半不能用。

### 3.3 答案键是推断的，且研究者供图带来伦理闸门

文件里不存答案，只能反推。所以数据模型必须有显式 `isAI` + `verifiedBy` 人工核验字段。

研究者供图带来一个必须处理的闸门：**人体组织/病理图在 UGent 这类机构通常需伦理确认**，且授权要覆盖三件**相互独立**的事：

1. **公网托管**（挂在大学网页上）
2. **现场公屏展示**（展台上的屏幕）
3. **裁剪与降质**（我们的流水线会改图 → 属衍生作品）

很多许可只允许其中一两项。所以三个 permit 字段分开记，**缺一项就不能达到 `status:"ready"`**。

---

## 4. 内容结构：三流模型

「后续引入的真图」与「对应的 AI 图」的对应关系必须是**显式且可校验的**，不能靠位置约定（那正是现在 deck 出问题的地方）。

```
data/real.csv   ──┐
                  ├──  tools/gen_manifest.py  ──→  data/manifest.js  ──→  引擎只看见 PUZZLES
data/ai.csv     ──┤        （校验 + join）           window.PUZZLES
data/pairs.csv  ──┘
```

| 册 | 一行是什么 | 关键字段 |
|---|---|---|
| `real.csv` | 一张**真图** | `realId`, 文件, 题材, credit, `permits.{web,publicDisplay,derivatives}`, `ethicsCleared` |
| `ai.csv` | 一张**AI 图** | `aiId`, 文件, **`derivedFromRealId`**, generator, model, prompt, 生成条款 |
| `pairs.csv` | **一道题** = 一次配对 | `puzzleId`, `realId`, `aiId`, 三轴难度, teaching, status |

### 三条由此而来的性质

**1. 对应关系可校验。** `ai.csv` 的 `derivedFromRealId` 是**反向引用**——AI 图自己记得它由哪张真图生成。于是能抓到一个真实存在的错误类别：**配错半边**（AI 图 X 本是照真图 Y 生成的，却配到了真图 Z 上）。这种错误光看题目对象看不出来，只有反向引用对得上才发现。

**2. 两路内容可以不对称地到达。** 研究者先给真图，AI 图稍后配对；或反过来。未配对的行**在磁盘上合法存在**，但不进 `manifest.js`、永不被 served。结构能容纳半成品。

**3. 一张真图可派生多道题——内容量的乘数。** 同一张真图生成多个 AI 变体（不同 `tells`/`postprocessing`）→ 一条 `realId` 对多条 `aiId` → 不同难度档的多道题。研究者供图是稀缺资源，这个结构让一张图撑起 easy 与 hard 两道题。

### 由此带来的两个必须处理的细节

- ⚠️ **抽题去重必须按 `realId`，不能按 `puzzleId`。** 否则一局里同一张真照片可能出现两次（配了不同 AI 变体）——既显得像 bug，又泄露答案（"上次这张是真的"）。`noRepeatWindow` 与本局已出集合都以 `realId` 为主键。
- ⚠️ **路径与文件名必须中性。** 语义化 id 只活在 CSV 里；落盘路径由 join 步骤重写为 `assets/img/p/<puzzleId>/1.webp` 和 `2.webp`。**绝不能出现 `assets/img/ai/xxx.webp`**——路径会出现在 URL、DOM、DevTools 里。

---

## 5. 难度模型：三条独立轴

难度**不是一个标签，而是三个独立的 1–5 分维度**，加权合成为一个连续分数，再分档。

| 轴 | 1 分 | 5 分 |
|---|---|---|
| `tells` 破绽明显程度 | 六指、乱码文字，一眼可见 | 毫无解剖与文字错误，只剩统计线索 |
| `subject` 题材复杂度 | 单一物体、平背景 | 多人遮挡 / 强透视 / 画面内文字 |
| `postprocessing` 抹平线索 | 两侧风格差异明显 | 真图被打磨到与 AI 同质感 |

### 为什么用三条轴而不是一个 hard 标签

因为**两条轴真的会互相抵消**：`{tells:5, subject:1, pp:1}`（简单场景里的细微错误）与 `{tells:1, subject:5, pp:5}`（复杂场景里的明显错误）**都落在 medium**。单一标签表达不了这种区别，而三轴可以——这正是它的价值。

### 权重放在 config，不放在题库

```js
// js/config.js
axisWeights: { tells: 0.45, subject: 0.30, postprocessing: 0.25 }
tierThresholds: { easy: 0.34, medium: 0.66 }   // 归一化分数的上界（含）
kidLevels: 5
```

于是**调难度曲线完全不用碰题库数据**。看到真实玩家之后重新调参是一行改动，而不是改 60 行题。

### 缺轴的处理

某条轴缺失时**计入分母但不计入分子**，于是"没评分的题"会变**更容易**，而不是悄悄重新分配权重给其它轴（那会让一道未评分的题看起来比实际更难）。

### 评分锚点必须写下来

`data/schema.md` 里放每根轴 1/3/5 分的**具体锚点描述**，否则两个人评同一张图会给不同分，三轴模型就失效了。

### JS 与 Python 必须一致

`difficulty.js` 会在 `tools/verify_assets.py --stats` 里用约 15 行 Python 重实现，并**与 `tests/test.difficulty.js` 共用同一张 fixture 表**。否则你调参时看的统计报告描述的题和游戏实际出的题不是同一批。

`--stats` 还打印**轴间相关系数**：若 `tells` 与 `subject` 相关系数 > 0.7，说明实际上只有一条轴，难度菜单是假的。

---

## 6. 抽题

- **带种子 RNG**（`mulberry32`）：牌堆可由种子复现 → bug 可工单化（"种子 12345，第 3 轮"），测试也不必 mock `Math.random`。
- **即时抽牌**而非预发牌 → 自适应升档能立刻生效。
- **去重按 `realId`**（见 §4）。
- **左右随机在 selector 层强制，不在渲染层。** selector 返回 `{puzzle, aiSlot}`，并拒绝任何让 AI 连续同侧超过 `avoidSameAiSideRun`(2) 轮的分配。**这一条使"永远选左"失效**——它直接消灭了现有 deck 的规律。
- **档位池必须能自动放宽**：严格池为空或耗尽时，每次放宽 0.10 至 0.40，最后退回全部已校验题。内容少时这条路径会一直触发，**它必须顺滑而不是报错**。

---

## 7. 状态机

```
boot → attract → menu → round → reveal → teach → (round | summary) → attract/menu
```

转移表是**纯数据**，与 DOM 副作用分离——这是它能不靠浏览器测试的原因。

**未知事件是 no-op + 警告，绝不改变状态。** 展台上卡死的 FSM 是最糟的一类 bug。

屏幕切换靠 `body[data-screen="round"]` 一个属性驱动 CSS，不用 JS 显隐；非活动屏幕因 `display:none` 自动离开无障碍树（不需要 `inert`）。

---

## 8. UI / UX

### 8.1 布局用宽高比查询，不用宽度断点

宽度断点会错两次：手机横屏（740×360）低于阈值但应并排；竖屏平板（900×1200）高于阈值但应堆叠。用视口**形状**：

```css
.board { grid-template-rows: 1fr 1fr; }                    /* 默认堆叠 */
@media (min-aspect-ratio: 1/1) {                            /* 至少方形即并排 */
  .board { grid-template-columns: 1fr 1fr; grid-template-rows: 1fr; }
}
```

三条硬规则：

1. **两张卡固定同一宽高比**（`aspect-ratio: 3/2` + `object-fit: cover`）——这是让残留形状差异变得无关的反作弊
2. **两卡像素尺寸完全一致**，永不出现"一张稍大"
3. **图片带显式 `width`/`height`**，冷启动 kiosk 不会中途重排

**屏幕是【恰好】一屏高，不是"至少"一屏高。** 答题屏用 `height: 100dvh` 而非 `min-height`。
`min-height` 遇到内容超出只会让 section 跟着长高，而 flex 容器没有确定高度，
就**没有"可分配空间"这个概念**——`.board` 上的 `flex: 1 1 auto` 连同它的 `min-height: 0`
全都无从发挥，整屏比视口高出一截，「下一题」被推出屏幕外（实测 1440×900 时
section 1071px、按钮 y=935，视口只有 900）。`overflow-y: auto` 是给极矮视口的兜底：
让**屏幕自己**滚，文档永远不滚。

空间不够时让路顺序是**板子优先**：`.teach-wrap` 的 `flex-shrink` 是 4（板子是 1）。
理由是不对称的——面板本来就能滚、按钮是 sticky 的，矮一点只是多滚两下；
板子被压扁是直接看不清图，那才是真的玩不了。两边各有一条下限：

- 板子 `min(34vh, 280px)`——再矮会被压成 0px，两张卡直接消失，**完全没法玩**
- 面板 `max(152px, 26vh)`——152px = sticky 按钮自己(104) ＋面板内边距(48)，再矮按钮被裁一截，
  而按钮被裁就等于又回到"够不着"

### 8.2 放大不是可选项

1080p 展台上卡片约 620×413px，要看清六指是不够的。**放大是让 3–5 档公平的前提**，不是锦上添花。

做成独立浮层而非卡片上的手势：卡右上角常驻放大镜（触摸命中区 64px）→ 全屏查看浮层，内部拖拽平移、双指缩放、双击切换 1×/2.5×，桌面端加跟随光标的圆形放大镜。浮层里带「选这张」按钮可就地作答。

**点选作答与平移缩放两个面永不重叠**——消除手势歧义，这是全年龄触摸 UI 最大的坑，也是做成浮层的主要理由。

### 8.3 揭晓不做翻牌

翻牌是直觉选择但在这里是错的：**镜像的照片无法阅读，会毁掉教学时刻。**

改用**色块 + 徽标 + 聚焦**，约 450ms：

| 时刻 | 动作 |
|---|---|
| t=0 | 被选中的卡加 3px 描边并抬升 2% —— 给触摸即时反馈 |
| t=120 | 两卡盖 12% 色块，徽标弹入：「AI」带闪光字形 /「ECHT」带相机字形 |
| t=300 | AI 卡加 4px 实色描边 + 轻微放大 1.02（**扁平化：用描边不用光晕**） |
| t=450 | 教学面板上滑，焦点移到面板标题（`tabindex="-1"`），`aria-live` 播报 |
| t=900 | 若有 `tellRegion`，面板里的 AI 图动画缩放并把该区域居中，画细环标出 |

最后一步**是整个设计里价值最高的功能**：它把"信我，手画错了"变成"你看这只手"。

`prefers-reduced-motion: reduce` 下全部时长归零。

#### ★ 真相与选择走两个独立通道

原计划把"这张图的真相"和"玩家选了哪张"都表达在卡片描边上。
实现时发现两者会**互相覆盖**：玩家选错时，"错"就只剩颜色可看，
而颜色是四条编码通道（字形 / 线型 / 粗细 / 颜色）里最不可靠的一条。

拆成两个互不干扰的通道：

| 通道 | 表达 | 实现 |
|---|---|---|
| 外框 `border` | **这张图的真相** | AI = 4px 实线琥珀；真图 = 2px 虚线青绿 |
| 内缩 `outline` | **玩家选了哪张** | 4px 蓝环，`outline-offset:-9px` + 白色内垫圈 |

粗细与线型都不同，所以**灰度打印也能读**；颜色只是冗余。白垫圈是必需的——
单色环压在照片上，遇到浅色区域会直接消失。

一眼可读的四种组合：**选对**（蓝环落在实线框上）、**选错**（蓝环落在虚线框上）、
**没选**（只有框没有环）、**没轮到**（两者皆无）。

结果字形只在**玩家真选了的那张**上出现。没作答时显示"错"太苛刻——
而且超时本来就不算答错。

### 8.4 教学面板

自上而下固定版式：状态与进度 → **聚焦到破绽的 AI 图** → 线索 chip → explanation（按语域）→ 方框强调的 **rule（下次怎么查）** → 【仅专家】可折叠的 prompt / 模型 / 真图出处 → **最大的「下一题」按钮**。

- **prompt 仅专家可见且折叠。** 对成人真的有趣，在儿童模式下**有害**——它把"找破绽"变成"跟描述对照"。
- 真图出处**仅在 web 模式屏显**；kiosk 上为了合规仍需展示，印在机器旁的实体牌上。
- 「下一题」**永远是最大的目标、永远是面板内 Tab 序第一**。因此它也是
  `position: sticky; bottom: 0` 的——这不是装饰：面板限高且 `overflow-y: auto`，
  长文案下按钮会落到滚动窗口之外（实测 1440×900 时曾在 y=1449，玩家要先滚 574px）。
  "最大的目标"必须**始终够得着**，否则这个称号没有意义。

### 8.5 作答限时 20 秒

**计时器只作用于作答，揭晓后立即停止**，教学面板不限时。否则"每题都教学"会被 20 秒吃掉。

UI 上是**扁平进度条**而非倒数数字——避免读秒焦虑。最后 5 秒转强调色。超时按答错处理但仍展示完整教学。`config.answerTimeLimitMs = 0` 可整体关闭。

### 8.6 无障碍

- **选择项绝不用 `<div>`**：每张卡是 `<button>`，`focus-visible` 3px 描边（默认描边在照片上根本看不见）
- **键盘**：`1`/`2` 或 `←`/`→` 选择，`Enter` 确认，`Space` 下一题，`Esc` 回菜单，`+`/`−` 缩放。板内 roving `tabindex` → 一轮只需 2 次 Tab 而非 8 次
- ⚠️ **`alt` 绝不能泄露答案**：`<img alt="Afbeelding A">`，用 `aria-describedby` 指向一个**揭晓前为空**的节点，揭晓后才填入真实描述。于是读屏用户与视力用户在同一时刻拿到同样信息。**这是反作弊要求与无障碍要求同向的唯一一处，也最容易因为"顺手先写好 alt"而写错。**
- **绝不只用颜色编码结果**：对/错同时靠字形（✓/✗）、描边样式（实/虚）、描边粗细区分。配色 **AI = 暖琥珀，真 = 冷青绿**——红绿对在三种色盲下都失效，这一对不会。发布前必过 DevTools 四种色觉模拟。
- **对比度**：正文 ≥4.5:1，大标题 ≥3:1，一律在实色背景上测量——**绝不把文字压在照片上**
- 触摸目标 ≥64px（kiosk ≥88px），间距 ≥16px；`aria-live` 播报节点必须 `.visually-hidden` 而非 `display:none`

### 8.7 Kiosk

- **模式由配置决定，不是由协议决定**：`mode = query.mode || (location.protocol==='file:' ? 'kiosk' : 'web')`。这样 kiosk 行为能在 http 下测。**UI 层绝不出现散落的 `if (kiosk)`**——这是"一份代码活两处"的关键
- **空闲 → attract**：45s 无输入回 attract。局中则先显示 12s 倒计时环再清除
- **会话清除是真的清除**：清内存 session、两张卡 `img.src=''`（释放已解码位图）、重置 zoom、清 `kiosk.` 前缀的 localStorage
- **全屏**必须由用户手势触发 → 在 attract 屏首次点击时请求；同时提供 `kiosk.bat` 用 `--kiosk` 启动（比 API 可靠）
- 抑制浏览器行为：`user-select:none`、`-webkit-tap-highlight-color:transparent`、`touch-action:manipulation`、`overscroll-behavior:none`、屏蔽右键
- **无外链**：dev 期断言遍历 DOM，kiosk 模式下发现跨域 `href` 就警告
- kiosk 默认静音

---

## 9. 占位图策略

要求比"没有图也能跑"更强：**占位图必须让难度分档真的可测**，所以它必须带**真实的、可控的破绽**。

`js/placeholder.js` 运行时生成 SVG `data:` URI，三轴是字面意义上的函数参数：

| tells | subject | pp | 占位图里实际画了什么 |
|---|---|---|---|
| 1 | 1 | 1 | 平底上一个物体。AI 版：六根手指且两侧都有拇指 |
| 3 | 3 | 2 | 两个交叠物体 + 投影。AI 版：多一个指节；投影与光源矛盾；文字字母镜像 |
| 5 | 5 | 5 | 三形体、遮挡、汇聚透视、画面内文字。AI 版：**不画显式缺陷**，只有光源方向不一致 + 噪声 + 模糊 |

`postprocessing` 对**两侧施加同等强度**，并给 AI 侧一点裁切。**这是这条轴的全部意义，且从第一天就可测。**

### 诚实护栏

`tells:5` 且不画缺陷时，占位图**并非真的可解**。所以生成器**最高只到 `tells:3`** 并标记 `verifiedSolution:false`，`validate.js` 据此把它排除在 `hard` 池外。

**占位图可以测所有档位的管道，但绝不能用它来调最高档的手感**——最高档必须用真实内容。

### 合并而非替换

真实题与占位题共存（`data/compose.js`），于是可以用 3 道真题 + 40 道占位题开发。**引擎分辨不出差别——这正是重点：如果引擎需要知道哪些是占位题，抽象就漏了。**

dev-only 的 `?placeholder=1` 强制全占位、`=0` 禁止占位。两者在发布构建里由一行 `location.search` 检查剥除，避免展台被乱输入的 URL 打进占位模式。

---

## 10. 已知风险与取舍

| 风险 | 状态 |
|---|---|
| **内容量是真正的瓶颈** | 7 组图铺 3 档只有约 2 道/档。真正的交付物不是"处理这 7 组"，而是**"让加入第 40 组题只要十分钟"**。§4 的"一图多题"是主要缓解手段 |
| **授权无法用代码解决** | `rightsCleared` 隔离能保证未清权的题不发布，但**不能创造本来不存在的权利** |
| **AI 图的生成器无法考证** | 现有 5 张 AI 图无元数据、无 C2PA、无 `Software` 标签，无法为叫不出名字的生成器记录条款。进公网应改用已知模型重新生成，或接受 `generator:"unknown"` 并写明 |
| **流水线会刻意降质真照片** | 这是最高档能成立的唯一办法，但**有些合作者会从原则上反对**。用 `postprocess.applied[]` 记录实际做了什么，让"意图"与"落盘字节"不会悄悄分叉 |

### 考虑过并否决：在清单里混淆答案键

kiosk 无法阻止一个决心够大的少年查看源码。可以把答案存成 id 的加盐哈希——**否决**：改盐会静默使人工核验过的 `isAI` 字段失效，把数据完整性问题变成密码学问题，动摇整个设计赖以成立的那一条保证。

**这是个诚实的游戏，不是安全游戏。** 学习才是目的，读源码的人不是威胁模型。

---

## 11. 验证策略

### 自动化（不需要人点页面）

纯逻辑模块（`difficulty` / `selector` / `scoring` / `machine` / `validate`）**零 DOM 访问、零 `Math.random`**（RNG 注入）。这个约束使整套测试无需框架即可运行，且**浏览器与 Node 双跑**。

最高价值的用例：

- `difficulty` — 阈值边界 0.34/0.66 ±1e-9；两个抵消用例（`{5,1,1}` 与 `{1,5,5}` 都应为 medium）；缺轴应变易而非 NaN；越界应 clamp
- `selector` — 固定种子下窗口内不重复；**按 `realId` 去重**；AI 侧不会连续同侧超过 2 轮；**只有 2 道题却要跑 5 轮时不抛异常**；严格池空时恰好放宽一次
- **配对完整性** — `pairs.csv` 引用的 id 必须存在；**`ai.derivedFromRealId` 必须等于该题配的 `realId`**（抓"配错半边"）；孤儿文件要报出来
- **i18n key 对齐** — `nl` 与 `en` 的 key 集合必须完全一致（专抓漏翻译）
- **题库完整性** — 恰好两张图、恰好一张 AI、三轴均在 1–5、`cue` 的 nl+en 齐全、**且 `src` 指向的文件在磁盘上真实存在**

### 手动清单（`file://` 那一步无可替代）

**验收测试就是：在展台机器上双击 `index.html`**，不是 `serve.bat`。然后整套再跑一遍 `serve.bat`——两条路径会分叉（service worker、share、localStorage）。

1. 从磁盘打开：DevTools **零控制台错误、零失败请求**。任何 `file://` CORS 消息都是发布阻断项
2. 全矩阵：{儿童, 专家} × {easy, medium, hard, 自适应} × {file://, http}
3. 纯键盘通关一局，不碰鼠标
4. 读屏冒烟：确认播报对错与教学文本，且**揭晓前 DOM 里没有东西指明哪张是 AI**
5. **轮次中查看源码**：`document.querySelectorAll('[data-is-ai]')` 必须返回空
6. 真平板：88px 目标、无双击缩放、无橡皮筋滚动、**浮层平移缩放不误触发作答**
7. 空闲：停 60s → attract；局中停 → 倒计时 → 清除；确认上一位玩家的痕迹从屏幕和 DOM 都消失
8. 四种色觉模拟下结果仍可读
9. 关闭系统动画：全部瞬时出现，没有卡在过渡中间
10. 缩放 200% + 360×640 视口：「下一题」不被裁切，板会堆叠
11. CPU 节流 6×：首轮 1s 内出现。下一轮的图要在教学面板期间用 `new Image()` 预热
12. 浸泡：`?autoplay=200` 跑 200 轮看内存是否增长（SVG `data:` URI 与解码位图是可疑泄漏点）
13. **纯鼠标通关一局**（不碰键盘）：点 attract 屏的**空白处**（不是文字）→ 选难度 → 开始 →
    点卡片 → 点「下一题」×5 → 点「再来一局」。**每一步都必须在视口内、无需滚动。**

    ★ 第 13 项是新增的，也是**最容易被整层跳过**的一项。2026-09-22 修掉的四个阻断缺陷
    全都只能由它发现：`.attract` 恒为可见（全屏面板盖住一切、点击全被吃掉）、
    事件委托不检查 root（点空白处没反应）、`[hidden]` 被作者样式盖掉、
    答题屏比视口高（「下一题」在 y=935 而视口只有 900）。
    当时 163 项测试、`?autoplay`、`--dump-dom` **全部是绿的**。

#### 已经能自动跑的（不需要人点页面）

- `node tests/run.js` → 163 项（纯逻辑，10 个文件）。
- `tests/run.html` → 176 项（多出的 13 项是 `test.dom.*`，要 `document`）。
  **两个入口都要跑**；`run.js` 会打印它跳过了哪些文件。
- 从磁盘打开：`chrome --headless=new --dump-dom "file:///…/index.html"` 读 `body[data-mode]` 与 `body[data-screen]`。
- **整局无人值守**：`?autoplay=12&seed=12345` 从 `file://` 跑完 attract → menu → 5 轮 → 总结，
  再 `--dump-dom` 检查总结屏内容是否完整。上面 12 项里有 4 项可以用这条路预先排掉。

下面手工清单里**第 3、4、5、8、9、10、13 项仍然无可替代**（真键盘、真读屏、真色觉模拟、
真系统动画开关、真缩放、真设备、真鼠标）——自动化能排掉的是崩溃与逻辑，
排不掉"看起来对不对"和"点下去有没有反应"。

**第 13 项原则上可以自动化**：浏览器 CDP 的 `Input.dispatchMouseEvent` 派发的是
真实输入事件，走完整的命中测试与事件传播，正是 `--dump-dom` 与 `?autoplay` 缺的那一层。
2026-09-22 的修复就是用这种方式验收的（16 项断言 + 8 个视口尺寸）。
该驱动目前**还未进仓库**——见 CHANGELOG 里的 `tools/smoke.mjs`。

---

## 12. 实现期做出的设计决定

计划里没写、但实现时必须定的。放在这里是因为**它们和 §8 的视觉与 §6 的抽题一样会约束后续改动**，
不是"实现细节"。

### 12.1 答案不进 DOM 是结构保证，不是纪律

`answer-reveal.buildSlot()` **不接收谜题对象、也不接收 `isAI`**，只接收一个图片地址和尺寸。
于是它**在结构上不可能**泄露答案——不是"我们小心没写"，而是"写不出来"。

徽标、结果字形这些节点在**揭晓时才创建**，而不是提前写好再 `display:none`。
理由和直觉相反：`display:none` 的元素**仍然**能被 DevTools、读屏和 CSS 选择器找到。
排查泄露时，"藏在 DOM 里"和"不在 DOM 里"是两件必须分清的事。

`audit()` / `assertClean()`（`?audit=1` 时每轮自动跑）是这条要求在**机器上可检验**的形式。

**而守卫自己也要被测。** 一个从不触发的守卫比没有守卫更糟——它给人虚假的安全感，
还占着"这条已经查过了"的位置。所以 `tests/test.dom.answer-reveal.js` 拿真实 DOM
逐一注入七种泄露属性与六种类名，确认**每一条都会响**；同时确认
`data-slot` / `data-zoom` 这类**槽位**属性**不会**误报——
误报一样要防，因为噪音会让真正的泄露被忽略。

### 12.2 谜题的两半可以不对称到达，但一道题必须同时齐备

`compose` 把真实题与占位题**合并**而不是替换。引擎分辨不出两者的差别——
**这正是重点：如果引擎需要知道哪些是占位题，抽象就漏了。**

一条推论：`hard` 档按设计只接受人工确认过可解的题（`meta.verifiedSolution:true`）。
占位图的 `tells` 最高只到 3，够不到 hard，于是**全部 10 道 hard 占位题在校验阶段就被排除出题池**。
这不是缺陷，是设计。它在控制台里的提示被单独点名，以免有人去修一个没坏的东西。

**题池的不变量：池里的每一道题都是我们愿意端出去的。** 校验不过的题直接出池，
而不是留在池里靠后面各处的判断去挡——挡漏一次，玩家就会看到一道没有讲解的题，
而那正好毁掉整个教学时刻。

### 12.3 卡片与放大按钮必须是兄弟

卡片本身是 `<button>`，放大按钮也是 `<button>`，而**按钮里不能套按钮**：
那是非法 HTML，浏览器会把内层按钮**拆出去**（DOM 结构与你写的不一样），焦点顺序随即崩坏。

所以是 `.card-slot`（网格项，负责尺寸）套着**兄弟** `.card` 与 `.zoom-btn`（绝对定位浮在右上角）。

尺寸一致由网格的 `1fr 1fr` 保证，**不是**两边各写一遍同样的样式——
后者迟早会有一边被改漏，而"一张稍大"本身就是提示。

### 12.4 视觉位置与 Tab 序是两件必须分开的事

教学面板里的「下一题」在 **DOM 里排第一位**（Tab 一下就够），
用 `.teach .btn-next { order: 99 }` 把它推到**视觉最下方**。

同理，放大镜的可见性用**内联 `display`** 控制，既不用 `hidden` 属性也不用 class 切换：
`hidden` 会被 `.loupe { display:block }` 这条类规则盖掉，
而 class 切换又得跟 `@media (hover:hover)` 打架。三个机制互相覆盖是这个组件最容易出的 bug。

**`hidden` 属性现在由一条全局规则兜底**：`base.css` 里的
`[hidden] { display: none !important; }`。上面那条不是 `.loupe` 独有的坑，而是通例——
浏览器默认的 `[hidden] { display:none }` 来自 **UA 样式表**，优先级低于**任何**作者样式，
所以只要元素身上有一个设了 `display` 的类（`.chip { display: inline-flex }`），
`hidden` 就**静默失效**。`#demo-flag` 就是这么坏的：它的文案是
"演示模式：这些是测试图片，不是真实谜题"，而库里明明有 7 道真题——**横幅在骗人**。

`.loupe` 仍保留内联 `display`：它要跟 `@media (hover:hover)` 协商可见性，
不只是"藏不藏"，是另一回事。

### 12.5 模式必须能在 http 下测

`mode = 查询串 ?mode= || (协议 === 'file:' ? kiosk : web)`。
**kiosk 行为因此可以在开发机上用 `?mode=kiosk` 测。**

如果模式是从协议硬推出来的，那么所有展台行为（空闲重置、清场、自动前进、静音）
在开发机上**永远测不到**，只能等到活动当天在展台上现场发现 bug。
`tests/test.modes.js` 守的就是这一条。

同理，UI 层里**不出现散落的 `if (kiosk)`**——所有模式差异在 `modes.settings()` 里
摊平成一个对象，UI 只读这个对象。这是"一份代码活两处"能成立的原因。

### 12.6 五个 UI 屏在一个文件里

`js/ui.js` 同时装着菜单、作答、揭晓、教学、总结五屏。

拆成五个文件**只增加一层间接、不增加清晰度**：它们共用同一个 `app` 对象、
同一组 DOM 引用、同一套 i18n 助手，而且揭晓与教学之间的状态交接非常频繁。
真正的边界在 `answer-reveal.js`（答案进 DOM 的唯一通道）和 `zoom.js`（手势），
那两个是独立文件。

### 12.7 启动器：绝对 URL 是必须的

`kiosk.bat` 最初写成 `cd` 到脚本目录 + 传相对路径 `index.html`。
**实测不成立**：Chrome 把命令行上的相对路径当成搜索词，打开的是一张错误页，
**而退出码仍是 0**——展会当天表现为"双击了，出来个空白页"，且没有任何错误信息。

必须拼绝对 `file://` URL。反斜杠换成斜杠；路径里的空格**不需要**转义（实测 Chrome 接受原样空格）。

另外 `--user-data-dir` 不是卫生习惯而是**功能必需**：Chrome 若已在用默认配置运行，
新进程只是往现有窗口丢一个标签页，命令行上的 `--kiosk` 会被**静默忽略**。

### 12.8 刻意不传 `--allow-file-access-from-files`

页面在 `file://` 下的全部写法（经典 script、题库是 `.js` 全局量、系统字体栈、storage 包装）
都是为了让它在**默认权限**下就能跑。

加上那个开关会把 CORS 限制关掉，于是哪天有人手滑引入了 ES module，
在装了启动器的机器上**仍然能跑**，到别的机器上才炸。宁可现在就炸。
